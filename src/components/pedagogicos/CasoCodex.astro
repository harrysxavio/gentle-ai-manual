---
---

<div class="comp-caso-codex" data-component="caso-codex" role="note" aria-label="Caso Codex">
  <div class="comp-caso-header comp-caso-codex-header">
    <span class="comp-caso-icon" aria-hidden="true">🟢</span>
    <strong>Codex</strong>
  </div>
  <div class="comp-caso-body">
    <slot />
  </div>
</div>

<style>
  .comp-caso-codex {
    border: 1px solid var(--sl-color-green-300);
    border-left: 4px solid var(--sl-color-green-500);
    margin: 1.5em 0;
    border-radius: 0 8px 8px 0;
    overflow: hidden;
  }

  :root[data-theme="dark"] .comp-caso-codex,
  :global([data-theme="dark"]) .comp-caso-codex {
    border-color: var(--sl-color-green-700);
    border-left-color: var(--sl-color-green-400);
  }

  .comp-caso-header {
    padding: 0.55em 1.1em;
    display: flex;
    gap: 0.5em;
    align-items: center;
    font-size: 0.85em;
  }

  .comp-caso-codex-header {
    background: var(--sl-color-green-50);
    color: var(--sl-color-green-700);
  }

  :root[data-theme="dark"] .comp-caso-codex-header,
  :global([data-theme="dark"]) .comp-caso-codex-header {
    background: var(--sl-color-green-950);
    color: var(--sl-color-green-300);
  }

  .comp-caso-icon {
    font-size: 1em;
    flex-shrink: 0;
  }

  .comp-caso-body {
    padding: 0.85em 1.1em;
    font-size: 0.93em;
    line-height: 1.6;
    color: var(--sl-color-text);
  }

  @media (max-width: 720px) {
    .comp-caso-codex {
      margin-left: 0;
      margin-right: 0;
      border-radius: 0 4px 4px 0;
    }
  }

  @media print {
    .comp-caso-codex {
      border-width: 1px;
    }
    .comp-caso-codex-header {
      background: none;
    }
  }
</style>
