---
import DefaultFooter from '@astrojs/starlight/components/Footer.astro';
import RouteContext from './RouteContext.astro';

const base = import.meta.env.BASE_URL;
---

<div class="curriculum-route-nav-wrapper">
  <RouteContext />
</div>
<DefaultFooter {...Astro.props} />

<script src={`${base}scripts/mermaid-zoom.js`} defer></script>

<style>
  .curriculum-route-nav-wrapper {
    max-width: var(--sl-content-width, 50rem);
    margin: 0 auto;
    padding: 0 var(--sl-content-padding-x, 1rem);
  }
</style>
